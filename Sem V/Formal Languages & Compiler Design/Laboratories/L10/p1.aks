// Verify if a number is prime.

var n;
n = readInt();
var prime = 1;
var d = 2;
while (d * d <= n && prime == 1) {
	if (n % d == 0) {
		prime = 0;
	};
	d = d + 1;
};
if (prime == 1) {
	print("prime");	
} else {
	print("not prime");
};